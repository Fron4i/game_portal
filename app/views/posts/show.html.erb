<h1><%= @post.title %></h1>

<article><%= @post.body %></article>

<section>
  <h2>Комментарии</h2>
  <ul>
    <% @comments.each do |comment| %>
      <li><%= comment.body %></li>
    <% end %>
  </ul>
  <%= pagy_nav(@pagy) if @pagy.pages > 1 %>
</section>
