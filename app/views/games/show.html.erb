<h1><%= @game.title %></h1>

<p><%= @game.description %></p>

<ul>
  <% @posts.each do |post| %>
    <li><%= link_to post.title, post_path(post) %></li>
  <% end %>
</ul>

<%= pagy_nav(@pagy) if @pagy.pages > 1 %>
