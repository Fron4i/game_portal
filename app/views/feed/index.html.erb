<h1>Моя лента</h1>

<% if @posts.empty? %>
  <p>Пока нет подписок или постов.</p>
<% else %>
  <ul>
    <% @posts.each do |post| %>
      <li><%= post.title %></li>
    <% end %>
  </ul>
  <%= pagy_nav(@pagy) if @pagy.pages > 1 %>
<% end %>
